{
  "name": "sync-5th-tab",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/backend",
    "apps/frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c \"blue.bold,green.bold\" \"npm run dev -w apps/backend\" \"npm run dev -w apps/frontend\"",
    "install:all": "npm install -w apps/backend -w apps/frontend",
    "build": "npm run build -w apps/backend && npm run build -w apps/frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
